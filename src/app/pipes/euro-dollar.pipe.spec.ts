import { EuroDollarPipe } from './euro-dollar.pipe';

describe('EuroDollarPipe', () => {
  it('create an instance', () => {
    const pipe = new EuroDollarPipe();
    expect(pipe).toBeTruthy();
  });
});
